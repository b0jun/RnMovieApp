import React from 'react';
import {SafeAreaView, Text} from 'react-native';

interface Props {}
const App = ({}: Props) => {
  return (
    <SafeAreaView>
      <Text>TEST</Text>
    </SafeAreaView>
  );
};

export default App;
